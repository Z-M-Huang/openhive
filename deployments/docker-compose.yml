version: "3.8"

services:
  openhive:
    build:
      context: ..
      dockerfile: deployments/Dockerfile
    environment:
      - OPENHIVE_IS_ROOT=true
    volumes:
      - ../data:/app/data
    ports:
      - "8080:8080"
